PROJEKT: CloudyWithAChanceOfFeedback
Region: eu-north-1 (Comprehend: eu-central-1)

------------------------------------------------------------
TAG 1 – Projekt & GitHub Setup
------------------------------------------------------------

- GitHub Repository erstellt (public)
- Repo-Name: cloudy-with-a-chance-of-feedback
- Lokales Cloning durchgeführt
- Ordnerstruktur angelegt:
  - frontend/
  - backend/
  - terraform/
  - k8s/
  - .github/
- .gitignore erstellt und erweitert
- README.md angelegt
- AWS Budget Alert eingerichtet
- Erster Commit + Push durchgeführt

Status: Abgeschlossen


------------------------------------------------------------
TAG 2 – Backend lokal starten
------------------------------------------------------------

- Spring Boot Projekt über Spring Initializr erstellt
- Java 17 verwendet
- Maven Projektstruktur
- H2 In-Memory Datenbank konfiguriert
- H2 Console aktiviert
- Security lokal deaktiviert (Development Mode)
- Erste Test-API erstellt (/api/test)
- Feedback Entity erstellt
- FeedbackRepository erstellt
- FeedbackController erstellt
- POST /api/feedback implementiert
- GET /api/feedback implementiert
- Speicherung in H2 erfolgreich getestet

Status: Abgeschlossen


------------------------------------------------------------
TAG 3 – AWS ML lokal testen
------------------------------------------------------------

- AWS SSO Login erfolgreich eingerichtet
- AWS SDK v2 integriert
- SSO + SSOOIDC Module eingebunden
- Region-Problem erkannt:
  - Comprehend nicht verfügbar in eu-north-1
  - Lösung: Comprehend in eu-central-1 gesetzt
- ComprehendService implementiert
- FeedbackService implementiert
- Sentiment-Analyse integriert
- Keyword-Extraction integriert
- 15 Testfeedback-Texte angelegt (POSITIVE / NEGATIVE / MIXED / NEUTRAL)
- ML Responses erfolgreich validiert
- Confidence Scores als echtes JSON formatiert
- Locale-Problem (Komma statt Punkt) gelöst mit Locale.US
- KeyPhrases als JSON Array gespeichert

Ergebnis:
- Sentiment wird korrekt erkannt
- Confidence wird korrekt als JSON gespeichert
- KeyPhrases werden korrekt extrahiert
- Backend vollständig ML-fähig

Status: Abgeschlossen


------------------------------------------------------------
TAG 4 – Terraform Bootstrap (Remote State)
------------------------------------------------------------

- Terraform Ordnerstruktur erweitert:
  - terraform/bootstrap/
- providers.tf erstellt
- main.tf erstellt
- variables.tf vorbereitet
- outputs.tf vorbereitet

- S3 Bucket für Remote State erstellt:
  - Name: cloudy-feedback-tfstate-eu-central-1
  - Versionierung aktiviert
  - Server-Side Encryption (AES256) aktiviert
  - Projekt-Tags gesetzt

- DynamoDB Tabelle für Terraform Locking erstellt:
  - Name: cloudy-feedback-terraform-locks
  - Billing Mode: PAY_PER_REQUEST
  - LockID als Hash Key definiert
  - Projekt-Tags gesetzt

- Terraform init erfolgreich durchgeführt
- terraform plan geprüft
- terraform apply erfolgreich ausgeführt
- Backend-Konfiguration auf S3 + DynamoDB umgestellt
- State Migration von lokal nach S3 erfolgreich durchgeführt
- Remote State in AWS Console validiert
- .gitignore geprüft (.terraform, *.tfstate ausgeschlossen)
- Commit + Push durchgeführt

Ergebnis:
- Terraform Remote State aktiv
- State liegt sicher in S3
- Locking via DynamoDB funktioniert
- Projekt entspricht Enterprise Best Practices
- Infrastruktur-Phase sauber gestartet

Status: Abgeschlossen

------------------------------------------------------------
TAG 5 – Terraform Network Layer
------------------------------------------------------------

- Network Modul erstellt:
  - terraform/modules/network/
  - main.tf
  - variables.tf
  - outputs.tf

- Root Terraform Struktur aufgebaut:
  - backend.tf (S3 + DynamoDB Remote State)
  - providers.tf (AWS Provider eu-central-1)
  - main.tf (Network Modul eingebunden)

- VPC erstellt:
  - CIDR: 10.0.0.0/16
  - Name: cloudy-feedback-vpc

- 2 Public Subnets erstellt:
  - eu-central-1a (10.0.1.0/24)
  - eu-central-1b (10.0.2.0/24)

- 2 Private Subnets erstellt:
  - eu-central-1a (10.0.101.0/24)
  - eu-central-1b (10.0.102.0/24)

- Internet Gateway erstellt und angebunden
- Public Route Table erstellt
- 0.0.0.0/0 Route auf IGW gesetzt
- Route Table Associations konfiguriert

- KEIN NAT Gateway verwendet (Projektregel v2)
- Remote State korrekt genutzt (network/terraform.tfstate)
- Terraform apply erfolgreich ausgeführt
- Commit + Push durchgeführt

Ergebnis:
- Netzwerkbasis für EKS und RDS steht
- Multi-AZ vorbereitet
- Infrastruktur Enterprise-konform modularisiert

Status: Abgeschlossen

------------------------------------------------------------
TAG 5 – Cognito OAuth + JWT Validation (Teil 2)
------------------------------------------------------------

- AWS Cognito User Pool final konfiguriert
- Authorization Code Flow per Browser validiert
- Token Exchange via curl + jq erfolgreich getestet
- access_token enthält cognito:groups Claim
- Spring Boot Resource Server aktiviert
- SecurityConfig produktionsreif angepasst:
  - /api/feedback nur für ROLE_ADMIN
  - Custom JwtAuthenticationConverter implementiert
  - cognito:groups → Authorities Mapping korrekt umgesetzt
- 200 Test mit ROLE_ADMIN erfolgreich
- 403 Test ohne ROLE_ADMIN erfolgreich
- Rollenprüfung serverseitig vollständig validiert
- JWT Decode-Regel im Master-Prompt ergänzt

Ergebnis:
- OAuth Flow vollständig funktionsfähig
- JWT Signaturprüfung korrekt
- Rollenprüfung produktionsreif umgesetzt
- Woche 1 Security-Ziel vollständig erreicht

Status: Abgeschlossen


------------------------------------------------------------
TAG 6 – Dockerisierung + ECR Integration
------------------------------------------------------------

- Dockerfile (Multi-Stage Build, Temurin 21) erstellt
- .dockerignore sauber konfiguriert
- Backend erfolgreich containerisiert
- Container lokal getestet (Port 8080, H2, Security 401 geprüft)
- Git-Commit-SHA als Docker-Tag etabliert (kein :latest)
- Zusammenhang Git SHA ↔ Docker Tag ↔ ECR ↔ Rollback vollständig verstanden
- Aktuellen Commit-SHA aus Git ausgelesen
- Docker Image mit Commit-SHA versioniert
- ECR Repository (eu-central-1) erstellt
- Docker Login zu ECR durchgeführt
- Versioniertes Image erfolgreich nach ECR gepusht
- Image in AWS Console validiert
- Commit-Format vereinheitlicht (ab Tag 6: "Tag X: Beschreibung")

Ergebnis:
- Versionierte Docker-Images bilden reproduzierbare Deployments
- Rollback-Fähigkeit durch gespeicherte Images gegeben
- Basis für EKS Deployment vorbereitet
- Woche 2 technisch sauber gestartet

Status: Abgeschlossen

------------------------------------------------------------
TAG 7 – EKS Cluster + ALB Controller
------------------------------------------------------------

- ECR Lifecycle Policy erstellt (Kostenkontrolle / Image Cleanup)
- EKS Modul-Struktur refactored:
  - modules/vpc/
  - modules/eks/
  - modules/security-groups/
- Security Group Modul ausgelagert (Projektvorgabe Struktur eingehalten)
- Terraform State bewusst neu aufgebaut (Modul-Umbenennung)

- EKS Cluster erstellt (eu-central-1)
- Managed Node Group konfiguriert:
  - desired=1
  - min=0
  - max=4
  - Public Subnets (laut Projektvorgabe)

- kubeconfig erfolgreich aktualisiert
- kubectl Verbindung zum Cluster validiert
- Node Status "Ready" erfolgreich geprüft

- OIDC Provider für EKS in IAM registriert
- IAM Policy für AWS Load Balancer Controller erstellt
- IAM Role for Service Account (IRSA) eingerichtet
- Helm installiert
- AWS Load Balancer Controller deployed
- Controller Pods im kube-system Namespace Running

- Node Group auf desired=0 skaliert (Kostenkontrolle)
- EC2 Instanz vollständig terminiert
- kubectl get nodes → No resources found

Ergebnis:
- Voll funktionsfähiger EKS Cluster
- Infrastruktur modular und projektkonform
- ALB Ingress vorbereitet
- Kostenkontrolle korrekt umgesetzt
- Woche 2 – Tag 3 vollständig abgeschlossen

Status: Abgeschlossen

------------------------------------------------------------
TAG 8 – RDS PostgreSQL (Terraform, Private Subnets)
------------------------------------------------------------

- RDS Modul erstellt:
  - terraform/modules/rds/
  - main.tf
  - variables.tf
  - outputs.tf

- DB Subnet Group implementiert (Private Subnets only)
- RDS PostgreSQL erstellt:
  - Engine: PostgreSQL 15
  - Instance: db.t3.micro (Projektvorgabe)
  - publicly_accessible = false
  - multi_az = false (kein Overengineering)

- Eigene RDS Security Group erstellt
- Ingress-Regel implementiert:
  - Nur EKS → RDS Port 5432
  - Kein 0.0.0.0/0 Zugriff

- Root Outputs ergänzt:
  - rds_endpoint
  - rds_port
  - rds_db_name

- Snapshot-Strategie verbessert:
  - skip_final_snapshot = false
  - random_id.snapshot_suffix eingeführt
  - final_snapshot_identifier mit stabilem Zufalls-Suffix
  - Begründung:
    - Verhindert Namenskollisionen
    - Vermeidet timestamp()-Plan-Drift
    - Stabiler Wert im Terraform State

- terraform fmt ausgeführt
- terraform validate erfolgreich
- terraform plan geprüft (5 Ressourcen)
- terraform apply erfolgreich durchgeführt
- RDS Endpoint erfolgreich validiert

Ergebnis:
- PostgreSQL läuft in Private Subnets
- Sicherheitsregeln minimal und korrekt
- Snapshot-Strategie kollisionsfrei und stabil
- Infrastruktur REQUIREMENTS-konform
- RDS Instanz am Tagesende gestoppt (Kostenkontrolle)

Status: Abgeschlossen

------------------------------------------------------------
TAG 9 – Unit Tests + Infrastruktur-Kostenkontrolle
------------------------------------------------------------

- FeedbackService Unit Tests erweitert
- ComprehendService Unit Tests implementiert
- Alle Tests erfolgreich ausgeführt (BUILD SUCCESS)

- Globaler Cost Audit durchgeführt
- EKS Cluster (module.eks) gezielt zerstört
- Keine ALBs, NAT Gateways oder Orphaned Volumes vorhanden
- RDS bleibt gestoppt (kein Snapshot erforderlich)

Ergebnis:
- Woche 2 fachlich vollständig abgeschlossen
- Infrastruktur kosten-sicher
- Projekt REQUIREMENTS-konform

Status: Abgeschlossen

------------------------------------------------------------
TAG 10 – Prod-Profil Vorbereitung + Kostenkontrolle
------------------------------------------------------------

- PostgreSQL Dependency (org.postgresql:postgresql) ergänzt
- application-prod.properties erstellt
- Prod-Profil für RDS-Anbindung vorbereitet
- Testlauf zur direkten RDS-Verbindung gestartet
- Test bewusst abgebrochen (EKS-Test erfolgt später strukturiert)
- EKS (module.eks) erneut via Terraform zerstört
- RDS gestoppt (keine produktiven Daten vorhanden)

Ergebnis:
- Prod-Profil technisch vorbereitet
- Infrastruktur wieder vollständig kostenneutral
- REQUIREMENTS-konforme Kostenkontrolle umgesetzt

Status: Abgeschlossen

------------------------------------------------------------
TAG 11 – Frontend Grundstruktur + Design-System
------------------------------------------------------------

- frontend/ Ordner initialisiert
- index.html erstellt (Public UI)
- admin.html erstellt (Admin Placeholder UI)
- style.css erstellt (zentrale Styles)
- assets/ Ordner angelegt
- logo-clean.png integriert
- Contained Header-Layout für Public & Admin implementiert
- Logo final gewählt (Variante B – SaaS-Style)
- Logo technisch tight gecroppt und integriert
- Header-Spacing mehrfach optimiert
- Button-Stretch-Problem (Flexbox) analysiert und sauber gelöst
- Public Feedback UI visuell finalisiert
- Admin Dashboard UI strukturell vorbereitet
- Einheitliches Farbkonzept (Purple + Emerald) definiert
- DESIGN-SYSTEM.md erstellt und verbindlich festgelegt

Ergebnis:
- Frontend-Grundstruktur vollständig versionierbar
- UI strukturell stabil und konsistent
- Design-Richtung verbindlich dokumentiert
- Vorbereitung für Cognito-Integration abgeschlossen

Status: Abgeschlossen

------------------------------------------------------------
TAG 12 – Cognito Login UI (Integration begonnen)
------------------------------------------------------------

- login.html erstellt (Admin Login Page)
- login.js implementiert
- Cognito Hosted Login erfolgreich aufgerufen
- Authorization Code Flow mit Testuser validiert
- OAuth Token Exchange erfolgreich getestet (curl)
- access_token / id_token / refresh_token erhalten

- Redirect-Problem mit Live Server analysiert
- Unterschied localhost vs 127.0.0.1 identifiziert
- Redirect URI Strategie für Dev/Prod definiert
- Lokale Entwicklungsumgebung angepasst (Python HTTP Server statt Live Server)
- Cognito Callback URL für localhost:8000 konfiguriert
- Cognito Login + Redirect erfolgreich getestet

Offene Punkte:
- Cognito Redirect mit Live Server final stabilisieren
- Token im Browser speichern
- Admin-Seite via Token schützen
- Cognito Logout implementieren

Ergebnis:
- Cognito Login grundsätzlich funktionsfähig
- Frontend Integration begonnen
- Admin Auth-Flow strukturell vorbereitet

Status: Teilweise abgeschlossen

------------------------------------------------------------
TAG 13 – Cognito Auth Flow finalisiert + Frontend Dockerisierung
------------------------------------------------------------

- Cognito App Client ohne Secret erstellt:
  - Name: cloudy-feedback-frontend
  - Authorization Code Flow für Browser angepasst

- Redirect URLs aktualisiert:
  - http://localhost:8080/login.html
  - http://localhost:8080/index.html

- login.js erweitert:
  - Authorization Code (?code=...) erkannt
  - Token Exchange gegen Cognito /oauth2/token durchgeführt
  - id_token / access_token im Browser gespeichert (sessionStorage)

- Admin-Seite geschützt:
  - Token-Prüfung vor Zugriff auf admin.html
  - Redirect zu login.html wenn kein Token vorhanden

- Logout implementiert:
  - sessionStorage.clear()
  - Cognito /logout Endpoint integriert
  - Redirect zurück zur Startseite

- Frontend Redirect-Port auf Docker-Setup umgestellt:
  - localhost:8000 → localhost:8080

- Frontend Dockerisierung umgesetzt:
  - Dockerfile erstellt (nginx:alpine)
  - Statische Dateien in /usr/share/nginx/html kopiert
  - Container-Port 80 freigegeben

- Docker Build erfolgreich getestet:
  - Image: cloudy-frontend
  - Container gestartet mit Port Mapping 8080:80

- Auth-Flow im Container getestet:
  - Cognito Login funktioniert
  - Token Speicherung funktioniert
  - Admin-Seite geschützt
  - Logout Flow funktioniert

Ergebnis:
- Vollständiger Frontend Auth Flow implementiert
- Cognito Integration produktionsreif
- Frontend Container lauffähig
- Grundlage für Kubernetes Deployment geschaffen

Status: Abgeschlossen

------------------------------------------------------------
TAG 14 – Kubernetes Manifeste + Container Registry Erweiterung
------------------------------------------------------------

- Kubernetes Manifeste erstellt:
  - backend-deployment.yaml
  - backend-service.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml
  - configmap.yaml
  - secret.yaml
  - ingress.yaml
  - hpa-backend.yaml

- Backend Deployment konfiguriert:
  - Image aus AWS ECR
  - Commit-SHA Tagging (kein :latest)
  - Environment Variablen über Kubernetes Secret
  - Ressourcenlimits passend für t3.small

- Kubernetes Health Checks implementiert:
  - Liveness Probe → /actuator/health
  - Readiness Probe → /actuator/health

- Spring Boot Actuator integriert:
  - Health Endpoint für Kubernetes Monitoring
  - Neues Backend Docker Image gebaut
  - Image mit Git Commit SHA getaggt
  - Image erfolgreich nach ECR gepusht

- Frontend Container Registry erweitert:
  - Eigenes ECR Repository erstellt
  - Frontend Image mit Commit-SHA getaggt
  - Image erfolgreich nach ECR gepusht

- Repository-Struktur verbessert:
  - .gitattributes von /backend nach Repository Root verschoben
  - frontend/.dockerignore erstellt

- Kubernetes Entwicklungsumgebung verbessert:
  - RedHat YAML Extension installiert
  - Kubernetes YAML Schema Mapping konfiguriert
  - YAML Autoformatierung beim Speichern eingerichtet

Ergebnis:
- Vollständige Kubernetes Manifeste implementiert
- Container Images für Frontend und Backend versioniert in ECR
- Projektstruktur bereinigt
- Infrastruktur bereit für Deployment auf EKS

Status: Abgeschlossen

------------------------------------------------------------
TAG 15 – Erstes EKS Deployment erfolgreich
------------------------------------------------------------

- EKS Cluster via Terraform gestartet
- Node Group aktiviert (t3.small, desired=1)

- Kubernetes Deployment durchgeführt:
  kubectl apply -f k8s/

- Frontend Pod erfolgreich gestartet
- Backend Pod gestartet, zunächst CrashLoopBackOff

Root Cause Analyse:

- Kubernetes Liveness / Readiness Probe schlug fehl
- Ursache: Spring Security blockierte /actuator/health
- Kubernetes erhielt HTTP 401 statt HTTP 200

Fix:

- SecurityConfig angepasst:
  .requestMatchers("/actuator/**").permitAll()

- Backend Image neu gebaut

Weiterer Fehler entdeckt:

- Docker Image Architekturproblem
- Mac M-Series baut ARM Images
- EKS Nodes laufen auf AMD64

Fehler im Cluster:

no match for platform in manifest

Fix:

docker build --platform linux/amd64

- Backend Image neu gebaut
- Image mit Git Commit SHA getaggt
- Image nach ECR gepusht
- Deployment auf neues Image aktualisiert

Verifikation:

kubectl get pods

backend 1/1 Running
frontend 1/1 Running

Ergebnis:

- Erstes vollständiges Deployment auf EKS erfolgreich
- Backend + Frontend laufen stabil im Cluster
- PostgreSQL Verbindung zu RDS erfolgreich
- Kubernetes Health Checks funktionieren
- Docker ARM → AMD64 Problem sauber gelöst

Status: Abgeschlossen

------------------------------------------------------------
TAG 16 – ALB Ingress, Public Deployment + CI/CD Pipeline
------------------------------------------------------------

ALB Ingress Infrastruktur

- AWS Load Balancer Controller vollständig installiert
- Helm Chart Deployment erfolgreich
- Controller Pods im kube-system Namespace validiert

kubectl get pods -n kube-system

aws-load-balancer-controller Running

- Kubernetes Ingress aktiviert
- Ingress Class auf ALB gesetzt

ingressClassName: alb

- Terraform IRSA Rolle für ALB Controller implementiert
- IAM Policy AWSLoadBalancerControllerIAMPolicy angebunden

- ALB automatisch durch Kubernetes Ingress erstellt
- Internet-facing Application Load Balancer validiert

aws elbv2 describe-load-balancers

- Öffentliche URL generiert

k8s-default-cloudyin-xxxxxxxx.eu-central-1.elb.amazonaws.com

Erster Zugriffstest

- Frontend über ALB erfolgreich erreichbar
- Anwendung erstmals öffentlich deployt

ALB Debugging

Initiale Probleme:

1. Webhook Fehler
   aws-load-balancer-webhook-service ohne Endpoints

2. Credential Fehler
   Controller konnte keine IAM Rolle beziehen

3. Backend Health Check Fehler

Root Cause:

Backend Health Endpoint geschützt durch Spring Security

Health Check Response:

HTTP 401

ALB erwartet:

HTTP 200

Fix:

Ingress Annotation ergänzt

alb.ingress.kubernetes.io/healthcheck-path: /actuator/health

Verifikation

aws elbv2 describe-target-health

Frontend Target:

State: healthy

Backend Target:

State: healthy

Ingress erfolgreich aktiv

kubectl get ingress

cloudy-ingress → ADDRESS gesetzt

ALB Routing validiert

/      → Frontend Service
/api   → Backend Service

Cognito Integration Problem erkannt

- Login über ALB funktionierte zunächst nicht
- Ursache:
  Cognito Callback URLs nur für localhost konfiguriert

Fix geplant für nächste Phase:

Callback URLs erweitern um

ALB URL + später CloudFront Domain

CI/CD Pipeline (GitHub Actions)

- GitHub OIDC Provider für AWS eingerichtet
- IAM Role erstellt:

GitHubActionsEKSDeployRole

- Trust Relationship mit GitHub OIDC konfiguriert
- AWS Policies angebunden:

AmazonEC2ContainerRegistryPowerUser
AmazonEKSClusterPolicy
AmazonEKSWorkerNodePolicy

- GitHub Actions Workflow erstellt

.github/workflows/main.yml

Pipeline Schritte:

1. Unit Tests ausführen
2. Docker Image bauen
3. Image nach ECR pushen (Tag = Git Commit SHA)
4. Deployment auf EKS
5. kubectl rollout status prüfen

Pipeline Debugging

Fehler 1:

AccessDeniedException
eks:DescribeCluster

Fix:

AmazonEKSWorkerNodePolicy an Role angehängt

Fehler 2:

kubectl Zugriff auf Cluster nicht erlaubt

Ursache:

GitHub Role nicht im aws-auth ConfigMap eingetragen

Fix:

aws-auth erweitert

GitHubActionsEKSDeployRole → system:masters

- aws-auth.yaml lokal exportiert als Referenz für zukünftige Cluster-Rebuilds

Deployment Pipeline erfolgreich

GitHub Actions Ergebnis:

Pipeline Status: SUCCESS

Deployment Verifikation

kubectl get pods

backend   Running
frontend  Running

Deployment Details geprüft

kubectl describe deployment backend

Image:

cloudy-feedback-backend:5809c5d

Liveness Probe:

/actuator/health

Readiness Probe:

/actuator/health

Ergebnis

- Vollständiger Public Kubernetes Deployment erfolgreich
- ALB Ingress Routing funktioniert
- AWS Load Balancer Controller stabil
- GitHub Actions CI/CD Pipeline funktionsfähig
- Automatisches Deployment auf EKS erfolgreich
- Infrastruktur weiterhin REQUIREMENTS-konform
- Kostenkontrolle aktiv (Node Group → 0, RDS gestoppt)

Status: Abgeschlossen

------------------------------------------------------------
TAG 17 – CloudFront, HTTPS + DNS Integration
------------------------------------------------------------

CloudFront Infrastruktur

- CloudFront Modul erstellt:
  terraform/modules/cloudfront/

- CloudFront Distribution per Terraform erstellt
- Architektur gemäß REQUIREMENTS.md umgesetzt:

Route53
↓
CloudFront
↓
ALB (EKS Ingress)
↓
Kubernetes Services

- ALB DNS automatisch aus Terraform State gelesen
  data.aws_lb.eks_ingress

- ALB als CloudFront Origin konfiguriert
- Origin Protocol Policy auf HTTPS gesetzt

HTTPS / Zertifikate

- ACM Zertifikat für ALB erstellt (eu-central-1)
- ACM Zertifikat für CloudFront erstellt (us-east-1)
- Zertifikatsvalidierung über Route53 DNS Records durchgeführt

CloudFront Konfiguration

- Default Cache Behavior konfiguriert
- Viewer Protocol Policy: redirect-to-https
- Cookies und Query Strings werden vollständig an ALB weitergegeben

Fix: CloudFront → ALB Verbindung

Initialer Fehler:

CloudFront konnte ALB nicht erreichen

Fehlerbild:

502 Bad Gateway

Root Cause:

CloudFront leitete den Host Header nicht korrekt an den ALB weiter.

Fix:

CloudFront Konfiguration angepasst:

headers = ["Host"]

Danach konnte CloudFront korrekt mit dem ALB kommunizieren.

Fix: API Routing über CloudFront

Initiales Problem:

API Requests über CloudFront funktionierten nicht zuverlässig.

Fehlerbilder:

502 Bad Gateway
503 Service Temporarily Unavailable

Root Cause:

CloudFront behandelte API Requests wie statische Inhalte
und leitete Requests nicht vollständig an den ALB weiter.

Fix:

Ordered Cache Behavior für:

/api/*

Konfiguration:

- Caching deaktiviert
- Alle HTTP Methoden erlaubt
- Cookies werden weitergeleitet
- Query Strings werden weitergeleitet
- Host Header wird weitergeleitet

Ergebnis:

API Requests werden korrekt von CloudFront
an den ALB und anschließend an das Backend
im EKS Cluster weitergeleitet.

DNS Integration

- Route53 Alias Record erstellt:

cloudy.cloudhelden-projekte.com
→ CloudFront Distribution

Verifikation

- HTTPS Zugriff erfolgreich getestet
- CloudFront liefert Frontend korrekt aus
- API Routing über /api funktioniert
- Globaler Edge Layer aktiv

Kostenkontrolle

- EKS Node Group wieder auf desired=0 gesetzt
- RDS Instanz bleibt gestoppt

Ergebnis

- Vollständige Production Architektur implementiert
- Globales CDN aktiv
- HTTPS vollständig integriert
- Domain Routing über Route53 + CloudFront stabil

Status: Abgeschlossen

------------------------------------------------------------
TAG 18 – Systemtests, Debugging und Kostenkontrolle
------------------------------------------------------------

Ziel:
System vollständig testen, Fehler identifizieren und beheben.
Kostenkontrolle nach Arbeitstag sicherstellen.

Frontend / Auth Debugging

- CloudFront lieferte weiterhin alte login.js Version aus
- Ursache: Browser Cache + CloudFront Cache Kombination
- Lösung:
  - CloudFront Invalidation durchgeführt
  - Browser Hard Reload (Cache leeren)
  - DevTools → Disable Cache aktiviert

Ergebnis:
Neue login.js Version wird korrekt ausgeliefert.

Redirect Problem behoben

Vorher:
const redirectUri = "http://localhost:8080/login.html";

Problem:
Login Redirect funktionierte nur lokal, nicht über CloudFront Domain.

Fix:
const redirectUri = window.location.origin + "/login.html";

Ergebnis:
Login funktioniert sowohl lokal als auch über:
https://cloudy.cloudhelden-projekte.com

Frontend Auth Verhalten geprüft

Startseite:

Nicht eingeloggt:
- Button zeigt "Admin Login"

Eingeloggt:
- Button zeigt "Admin"
- zusätzlicher "Logout" Button wird dynamisch erstellt

Navigation:

Admin → Startseite → Admin funktioniert korrekt.

Logout Verhalten getestet:

- Logout von Admin Seite funktioniert
- Logout von Startseite funktioniert
- SessionStorage wird korrekt gelöscht
- Cognito Logout Redirect funktioniert

Backend Debugging begonnen

API Test mit curl durchgeführt:

GET
/api/feedback

Ergebnis:
401 Unauthorized

Nach Login Test mit Bearer Token:

Ergebnis:
500 Internal Server Error

Analyse:

- Backend versucht Feedback Daten zu laden
- RDS Tabelle existiert wahrscheinlich noch nicht
- Datenbank Schema muss noch erstellt werden

Frontend Feedback Formular getestet

Feedback über Formular abgesendet.

Ergebnis:

- Keine Anzeige im Admin Dashboard
- Aktualisieren Button zeigt keine Daten

Analyse:

Mögliche Ursachen:

- Datenbank Tabelle fehlt
- Backend Endpoint liefert Fehler
- Statistik Endpoint eventuell noch nicht implementiert

Weitere Tests geplant für nächsten Arbeitstag.

Kostenkontrolle (Ende Arbeitstag)

RDS:

- RDS Instanz gestoppt um Kosten zu vermeiden

EKS:

Node Group über Terraform auf 0 skaliert.

Datei:
terraform/modules/eks/main.tf

Änderung:
desired_size = 0

Terraform ausgeführt:

terraform plan
terraform apply

Ergebnis:

- Worker Nodes werden terminiert
- kubectl get nodes zeigt nach kurzer Zeit keine Nodes mehr

Kostenstatus nach Arbeitstag:

- RDS: gestoppt
- EKS Nodes: 0
- Nur EKS Control Plane läuft (~$0.10/h)

Status:
System grundsätzlich funktionsfähig, jedoch Backend API noch nicht vollständig getestet.
Weitere Debugging- und Systemtests geplant für nächsten Arbeitstag.

------------------------------------------------------------
AKTUELLER PROJEKTSTATUS
------------------------------------------------------------

Projekt:
CloudyWithAChanceOfFeedback

Region:
eu-central-1 (Hauptregion)
us-east-1 (ACM für CloudFront)

Architektur:
Cloud-native Anwendung auf AWS mit Kubernetes (EKS)

Infrastruktur (Terraform):
- VPC mit Public und Private Subnets über 2 Availability Zones
- Internet Gateway und Routing eingerichtet
- Security Groups für ALB, EKS Nodes und RDS
- EKS Cluster mit Node Group
- RDS PostgreSQL Datenbank (db.t3.micro) in Private Subnet
- CloudFront Distribution vor dem ALB
- ACM Zertifikat in us-east-1 für CloudFront
- Route 53 Domain: cloudy.cloudhelden-projekte.com
- Remote Terraform State in S3
- State Locking über DynamoDB

Kubernetes (EKS):
- Frontend Deployment (Nginx)
- Backend Deployment (Spring Boot)
- ClusterIP Services für interne Kommunikation
- Ingress (AWS Load Balancer Controller → ALB)
- HorizontalPodAutoscaler für Backend
- Liveness und Readiness Probes konfiguriert
- Resource Requests und Limits definiert

Container / Registry:
- Docker Images für Frontend und Backend
- Images werden in Amazon ECR gespeichert
- Image Tags basieren auf Git Commit SHA

CI/CD Pipeline (GitHub Actions):
Pipeline bei jedem Push auf main:

1. Unit Tests ausführen
2. Docker Images bauen
3. Images in ECR pushen
4. Kubernetes Deployment auf EKS aktualisieren
5. Rollout Status prüfen
6. CloudFront Cache Invalidation durchführen

Authentifizierung:
- AWS Cognito User Pool eingerichtet
- Admin Gruppe erstellt
- JWT Token Validierung im Backend aktiviert
- Frontend Login über Cognito Hosted UI

Frontend:
- Öffentliche Startseite mit Feedback Formular
- Admin Login Seite
- Admin Dashboard
- Logout Funktion über Cognito
- Dynamische Navigation (Login / Admin / Logout)
- Redirect funktioniert lokal und über CloudFront Domain

Backend:
- Spring Boot REST API
- Endpoint zum Einreichen von Feedback
- Endpoint zum Abrufen von Feedback
- Integration mit Amazon Comprehend
  - Sentiment Analyse
  - Key Phrase Extraktion

ML Integration:
- Amazon Comprehend detectSentiment()
- Amazon Comprehend detectKeyPhrases()
- Ergebnisse werden im Feedback Datensatz gespeichert

CloudFront:
- CDN vor dem ALB
- HTTPS über ACM Zertifikat
- API Pfade nicht gecached

Aktueller Teststatus:

Login / Auth:
- Cognito Login funktioniert
- Logout funktioniert
- Redirect funktioniert lokal und über CloudFront

Frontend:
- Startseite lädt korrekt
- Navigation funktioniert
- Admin Dashboard erreichbar

Backend:
- API erreichbar
- Authentifizierte Requests funktionieren
- Datenbankzugriff aktuell noch in Debugging

Feedback System:
- Formular sendet Requests an Backend
- Speicherung und Statistik werden aktuell getestet

Kostenstatus:

RDS:
- Instanz gestoppt

EKS:
- Node Group auf desired_size = 0 skaliert (Terraform)

Aktive Kosten aktuell:
- nur EKS Control Plane (~$0.10/h)

Projektphase:
Woche 4 – Systemtests, Debugging und Vorbereitung der Präsentation

Nächste Schritte:
- Backend Datenbank Schema prüfen
- Feedback Speicherung testen
- Statistik Endpoint implementieren / testen
- Admin Dashboard vollständig testen
- Vollständige Systemtests durchführen
- Demo Daten vorbereiten

------------------------------------------------------------
OPEN TECH TASKS
------------------------------------------------------------

Backend

- Datenbank Schema prüfen / erstellen
  Tabelle "feedback" muss existieren

  Erwartetes Schema:
  feedback(
    id,
    text,
    created_at,
    sentiment,
    confidence_json,
    key_phrases_json
  )

- JPA Konfiguration prüfen
  sicherstellen dass Hibernate Tabellen automatisch erstellen kann
  (spring.jpa.hibernate.ddl-auto=update)

- Feedback Speicherung testen
  POST /api/feedback muss Daten korrekt in RDS speichern

- Feedback Abruf testen
  GET /api/feedback muss gespeicherte Daten zurückgeben

- Statistik Endpoint implementieren
  GET /api/feedback/stats

  Funktion:
  Statistik der letzten 7 Tage

  Erwartete Rückgabe:
  - positive
  - negative
  - neutral
  - mixed


Frontend

- Feedback Formular vollständig testen
  prüfen ob Request korrekt an Backend gesendet wird

- Feldmapping prüfen
  Frontend sendet aktuell "message"
  Backend erwartet "text"

- Admin Dashboard Datenanzeige testen
  Feedback Liste anzeigen

- Aktualisieren Button im Admin Dashboard testen
  muss neue Daten vom Backend laden


Systemtests

- End-to-End Test durchführen
  Browser → CloudFront → ALB → Backend → RDS

- API Tests mit curl durchführen
  POST /api/feedback
  GET /api/feedback
  GET /api/feedback/stats

- Authentifizierung testen
  Admin Dashboard nur mit gültigem Cognito Token erreichbar


CI/CD

- Pipeline Run nach Änderungen prüfen
  Test → Build → ECR Push → Deploy → Rollout

- Kubernetes Deployment prüfen
  sicherstellen dass neue Images mit Commit SHA laufen

- Pod Status prüfen
  kubectl get pods

- Image Version prüfen
  kubectl describe deployment frontend
  kubectl describe deployment backend


Präsentationsvorbereitung

- vollständige Systemtests durchführen
- Demo Daten erstellen (10–15 Feedback Beispiele)
- Admin Dashboard mit realistischen Daten testen
- Präsentations-Demo vorbereiten

------------------------------------------------------------
DOKU-MERKLISTE (für Woche 4)
------------------------------------------------------------

README.md

- Projektbeschreibung ergänzen
- Architekturübersicht der Anwendung erklären
- verwendete AWS Services dokumentieren
- Setup Anleitung hinzufügen:
  - Voraussetzungen
  - lokale Entwicklung
  - Deployment Ablauf

ARCHITECTURE.md

- Architekturdiagramm erstellen
- folgende Komponenten darstellen:

  Benutzer
  ↓
  CloudFront
  ↓
  ALB (AWS Load Balancer Controller / Ingress)
  ↓
  EKS Cluster
    ├ Frontend Pod (Nginx)
    └ Backend Pod (Spring Boot)
  ↓
  RDS PostgreSQL

- zusätzliche AWS Services dokumentieren:

  Cognito (Authentication)
  ECR (Container Registry)
  Comprehend (Sentiment Analyse)

Terraform Dokumentation

- Modulstruktur erklären:

  terraform/
    modules/
      vpc/
      eks/
      rds/
      cognito/
      security-groups/
      cloudfront/

- erklären warum Infrastruktur als Code genutzt wird
- Terraform State Management erklären (S3 + DynamoDB)

Kubernetes Dokumentation

- Deployment Struktur erklären
- Service Typen erklären (ClusterIP)
- Ingress erklären (ALB Integration)
- HorizontalPodAutoscaler kurz erläutern
- Liveness und Readiness Probes erklären

CI/CD Pipeline

- GitHub Actions Workflow erklären
- Pipeline Schritte dokumentieren:

  Test
  Build
  ECR Push
  Deploy auf EKS
  Rollout Status prüfen
  CloudFront Cache Invalidation

AWS ML Service Dokumentation

- Amazon Comprehend erklären
- Sentiment Analyse Funktionsweise beschreiben
- Key Phrase Extraktion erklären
- Integration im Backend dokumentieren

Kostenkontrolle

- erklären warum Ressourcen regelmäßig gestoppt werden
- Node Group Scaling Strategie dokumentieren
- RDS Stop / Snapshot Strategie erklären

Präsentation vorbereiten

- Architektur Slide erstellen
- Terraform Struktur erklären
- Kubernetes Deployment zeigen
- CI/CD Pipeline Lauf demonstrieren
- Live Demo vorbereiten:

  1. Feedback absenden
  2. Sentiment Analyse zeigen
  3. Admin Dashboard anzeigen
  4. Statistik aktualisieren

========================================
DESTROY-LOG
========================================
2026-03-01
- Zerstörte Module: module.eks
- Grund: Kostenkontrolle gemäß REQUIREMENTS.md
- RDS Snapshot: nicht erforderlich (RDS gestoppt)
----------------------------------------
2026-03-02
- Zerstörte Module: module.eks
- Grund: Testlauf abgebrochen – Kostenkontrolle gemäß REQUIREMENTS.md
- RDS Snapshot: nicht erforderlich (keine produktiven Daten)
----------------------------------------
2026-03-08
- Zerstörte Module: module.eks
- Grund: Tagesende – Kostenkontrolle gemäß REQUIREMENTS.md
- RDS Snapshot: nicht erforderlich (RDS gestoppt)
----------------------------------------
